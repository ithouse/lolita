<% 
  value = self.instance_variable_get("@#{resource_name}").send("#{field.name}")
  value = value.strftime(field.format) if field.format
%>
<%= text_field resource_name, field.name, {:value=>value} %>
